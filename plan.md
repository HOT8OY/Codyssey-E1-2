# 🎯 나만의 퀴즈 게임 구현 계획서 (MVC 구조)

## 1. 프로젝트 개요
터미널에서 동작하는 Python 기반의 퀴즈 게임입니다. 사용자 요구사항에 따라 **MVC(Model-View-Controller) 디자인 패턴**을 적용하여 코드를 구조화하며, **모든 보너스 기능(랜덤 출제, 문제 수 선택, 힌트 기능, 퀴즈 삭제, 점수 기록 히스토리)**을 포함하여 구현합니다.

---

## 2. MVC 구조 설계
현재 생성되어 있는 파일들을 기반으로 역할을 명확히 분리합니다.

### 2.1 Model (`quiz_model.py`)
데이터의 상태와 비즈니스 로직을 담당합니다.
- **`Quiz` 클래스**: 개별 퀴즈 데이터를 정의하는 클래스
  - 속성: `question`(문제), `choices`(선택지 리스트), `answer`(정답 번호), `hint`(힌트 문자열)
- **`QuizManager` 클래스**: 전체 데이터를 관리하는 클래스
  - 데이터(퀴즈 목록, 최고 점수, 점수 히스토리)를 메모리에 보관
  - `state.json` 파일 읽기/쓰기 (파일이 없거나 손상되었을 때의 예외 처리 포함)
  - 퀴즈 추가, 삭제, 목록 조회, 점수 갱신 등의 로직 처리

### 2.2 View (`quiz_view.py`)
사용자에게 보여지는 화면(출력)과 사용자로부터 입력을 받는 기능(입력)을 담당합니다. 데이터 처리 로직은 포함하지 않습니다.
- **`QuizView` 클래스 (또는 기능별 함수 모음)**:
  - 메인 메뉴 출력 및 입력 받기
  - 퀴즈 출제 화면 (문제, 선택지 출력 및 정답 입력)
  - 힌트 출력 화면
  - 게임 결과 및 점수 출력
  - 에러 및 안내 메시지 출력 (잘못된 입력 처리 안내 등)

### 2.3 Controller (`quiz_controller.py`)
Model과 View 사이의 연결고리 역할을 합니다.
- **`QuizController` 클래스**:
  - `QuizManager(Model)`와 `QuizView(View)`의 인스턴스를 생성
  - 사용자의 메뉴 선택에 따라 적절한 Model의 메서드를 호출하고, 그 결과를 View에 전달하여 화면을 갱신
  - 메인 실행 루프 담당

### 2.4 Main (`main.py`)
프로그램의 진입점입니다. `QuizController`를 실행하여 프로그램을 시작합니다.

---

## 3. 데이터 구조 (`state.json`)
보너스 요구사항인 '점수 기록 히스토리' 및 '힌트 기능'을 반영하기 위해 아래와 같이 스키마를 구성합니다.

```json
{
    "quizzes": [
        {
            "question": "영화 '기생충'의 감독은?",
            "choices": ["박찬욱", "봉준호", "김기덕", "이창동"],
            "answer": 2,
            "hint": "봉으로 시작합니다."
        }
    ],
    "best_score": 80,
    "history": [
        {
            "date": "2026-08-09 15:30:00",
            "played_count": 5,
            "score": 80
        }
    ]
}
```

---

## 4. 단계별 구현 순서

1. **[1단계] Model 구현 및 파일 I/O**: `quiz_model.py`에 `Quiz` 및 `QuizManager` 뼈대 구축. 더미 데이터를 활용해 `state.json` 읽기/쓰기 기능 우선 완성
2. **[2단계] View 입출력 틀 잡기**: `quiz_view.py`에 메인 메뉴, 퀴즈 목록, 에러 메시지 등 화면 출력용 메서드 구현
3. **[3단계] Controller 연결 및 메인 루프 구성**: `quiz_controller.py`에 컨트롤러를 작성하고 1, 2단계를 연결. 종료 기능 및 잘못된 메뉴 입력 방어 코드 추가
4. **[4단계] 필수 기능 구현**: 퀴즈 목록 보기, 퀴즈 추가 기능 완성
5. **[5단계] 퀴즈 풀기 기본 기능 구현**: 퀴즈 출제, 정답 비교, 점수 계산 기능 완성
6. **[6단계] 보너스 기능 구현**: 
   - 힌트 추가 및 힌트 사용 시 점수 차감 로직
   - 퀴즈 랜덤 출제 및 풀 문제 수 지정
   - 퀴즈 삭제 기능
   - 히스토리 저장 및 점수 확인 시 출력 기능 구현
7. **[7단계] 리팩토링 및 예외 처리 고도화**: 공통 입력 예외(공백, 문자 등) 처리 강화, 비정상 종료(Ctrl+C) 방어 코드 추가

---

## 💡 개발 파트너(AI)가 사용자에게 묻는 질문들
저희가 함께 코드를 짜기 전에, 몇 가지 논의하고 싶은 점이 있습니다!

1. **점수 차감 로직 기준**: 보너스 기능인 '힌트 사용 시 점수 차감'과 관련해서, 1문제 당 기본 점수를 몇 점으로 설정하고, 힌트를 볼 때 몇 점을 깎을까요? (예: 1문제 20점 만점 / 힌트 시 10점 차감)
2. **기본 제공 퀴즈의 주제**: 요구사항에 본인이 선택한 주제의 퀴즈 5개 이상을 작성하라고 되어 있습니다. 어떤 주제(예: 파이썬 문법, 영화, 상식 등)로 기본 퀴즈를 만들까요?
3. **구현 방식 진행**: 전체 틀을 먼저 제가 짜드리고 세부 구현을 직접 채워보실래요? 아니면 1단계부터 차근차근 제가 가이드를 드리며 진행해볼까요?
